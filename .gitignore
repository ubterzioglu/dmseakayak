.omc/
.DS_Store
*.log
node_modules/

# Build çıktısı
dist/
*.tsbuildinfo

# Secrets — ASLA commit edilmez
.env
.env.local
.env.*.local

# Large source archives — never commit
docs/GORSEL/*.zip

# Footer source video (optimized copy lives in public/videos/)
docs/footervideo.*
docs/herovideo.*

# Footer source video (compressed copy lives in public/videos/)
newfooter.mp4

# Root-level logo sources (optimized copies live in public/)
/kenarliklilogo.png
/maskotbig.png
/ognew.png
/transparanlogo.png
