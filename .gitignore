.omc/
.serena/
.DS_Store
*.log
node_modules/

# Build çıktısı
dist/
*.tsbuildinfo

# Secrets — ASLA commit edilmez
.env
.env.local
.env.*.local

# Large source archives — never commit
docs/GORSEL/*.zip

# Review screenshot backups — reference only, not needed in repo
docs/YORUMLAR/

# Footer source video (optimized copy lives in public/videos/)
docs/footervideo.*
docs/herovideo.*

# Footer source video (compressed copy lives in public/videos/)
newfooter.mp4

# Hero source video (rotated + compressed copy lives in public/videos/heronew.mp4)
hero.MOV
hero.mov
heronew.MP4

# Logo sources (optimized copies live in public/)
docs/kenarliklilogo-source.png
docs/maskotbig-source.png
docs/ognew-source.png
